<%= form_for(@activation_key) do |f| %>
  <% if @activation_key.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activation_key.errors.count, "error") %> prohibited this activation_key from being saved:</h2>

      <ul>
      <% @activation_key.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
